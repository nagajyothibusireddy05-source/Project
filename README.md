# Project
A lightweight Expense Tracker built completely using React.js, React Hooks, JSX, and LocalStorage, but without any Node.js, npm, or build tools. The entire project runs inside a single HTML file using React &amp; Babel CDNs, making it extremely easy to deploy, share, and use.
